<% include header %>
<% if( typeof messageError !== 'undefined'){ %>
        <div class="alert alert-danger">
          <%= messageError %>
        </div>
<% } %> 
<% if (typeof messageSuccess !== 'undefined'){ %>
        <div class="alert alert-success">
          <%= messageSuccess %>
        </div>
<% } %>
<br/>

<div class="container">
    <h2>Mon compte</h2>
      <p class="lead"> Nom : <%= utilisateurs.nom %> </p>
      <p class="lead"> Prénom : <%= utilisateurs.prenom %> </p>
      <p class="lead"> Mail : <%= utilisateurs.mail %> </p>
      <p class="lead"> Téléphone : <%= utilisateurs.tel %> </p>
      <p class="lead"> Rôle : <%= utilisateurs.id_role.nom_role %> </p>
      <p class="lead"> Service : <%= utilisateurs.id_service.nom_service %> </p>
        </div> 
        <a href='/'> <button type="button" class="btn btn-default">Retour</button></a>
</div>
<% include footer %>
